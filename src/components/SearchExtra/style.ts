import styled from 'styled-components';

const SearchExtraContainer = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  border-radius: 2px;
  background-color: white;
  box-shadow: 0 2px 4px 0 rgb(0 0 0 / 50%);
`;

export default { SearchExtraContainer };
